name: KiCad Release (on merge to main)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: read

jobs:
  call:
    uses: inkfish-hw/PCB-actions/.github/workflows/kicad-action.yml@main
